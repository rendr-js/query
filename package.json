{
  "name": "@rendrjs/query",
  "version": "0.0.8",
  "description": "Official query hooks for Rendrjs",
  "main": "dist/index.js",
  "scripts": {
    "pub": "npm version patch && npx tsc && npm publish",
    "test": "vitest"
  },
  "peerDependencies": {
    "@rendrjs/core": "^0.2.48"
  },
  "license": "ISC",
  "devDependencies": {
    "@rendrjs/testing-library": "^0.0.28",
    "jsdom": "^24.0.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.6",
    "vitest": "^1.0.2"
  }
}
